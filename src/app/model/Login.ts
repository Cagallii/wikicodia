export default class Login {

  //A revoir avec l'authentification accessToken et refreshToken
    public id: string;
    public username: string;
    public accessToken: string;
    public refreshToken: string;
  }